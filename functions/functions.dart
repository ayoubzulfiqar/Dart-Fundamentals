import '../classes/extentions_class.dart';

void main() {
  // function are 'self contain modulus of code' that perform the specific task
  // function usually take the data process it and return it as result.
  // once a function is written it can be used as over and over again.
  // function can be called into another function
  // function use to organize code break code and and make it reuse-able

  // The main is pre-define function in dart which is entry point to the application
  // the dart script need main() function for execution

  // sum(list);
}

// void average(List<int> list) {
//   list = [1, 2, 3, 4, 5];
//   int sum = 0;
//   for (var i in list);
//   sum += list[i];
// }
